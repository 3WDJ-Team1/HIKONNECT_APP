<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.group_list.groups_list_main">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/Theme.AppCompat.Light">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/transparent"
            app:contentInsetStartWithNavigation="0dp">

            <ImageButton
                android:id="@+id/btnGoBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:padding="24dp"
                android:background="@color/transparent"
                android:src="@drawable/ic_left_arrow_angle_svgrepo_com"
                android:tint="@color/grey_300"
                android:visibility="visible"/>

            <TextView
                android:id="@+id/joinedGroupToolbarTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="8dp"
                android:layout_marginStart="30dp"
                android:textSize="20sp"
                android:textColor="@color/black"
                android:textStyle="bold"
                android:text="내 기록 보기"/>

        </android.support.v7.widget.Toolbar>

    </android.support.design.widget.AppBarLayout>

    <include layout="@layout/user_records"
        android:id="@+id/layout_my_record" />

</android.support.design.widget.CoordinatorLayout>